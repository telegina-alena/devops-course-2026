Удобно в VS Code:

Визуальное разрешение merge-конфликтов.

Панель Source Control для коммитов и просмотра изменений.

Графическая история коммитов и веток (Git Graph / GitLens).

Удобнее в терминале:

Интерактивный rebase (git rebase -i) и другие сложные операции (reflog, cherry-pick, reset).
